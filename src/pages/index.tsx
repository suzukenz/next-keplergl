import InputToken from '~/components/InputToken'
import Layout from '~/components/Layout'

const Home: React.FC = () => {
  return (
    <Layout>
      <InputToken />
    </Layout>
  )
}

export default Home
